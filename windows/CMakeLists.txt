cmake_minimum_required(VERSION 3.14)
project(ahorrapp LANGUAGES CXX)

set(BINARY_NAME "ahorrapp")
set(APPLICATION_ID "dev.jmcerezo.ahorrapp")

# ... rest of the file usually follows standard flutter template
# I'll only update the project and binary names for now as I don't want to overwrite the whole file
# without reading it first, but I'll use the search and replace logic mentally.
